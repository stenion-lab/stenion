{
  "name": "@stenion/indexer",
  "version": "0.0.0",
  "private": true,
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "typecheck": "tsc -p tsconfig.json",
    "start": "node dist/index.js",
    "test": "node --test \"src/**/*.test.ts\""
  },
  "dependencies": {
    "@stenion/adapters": "workspace:*",
    "@stenion/core": "workspace:*",
    "@stenion/db": "workspace:*"
  }
}
