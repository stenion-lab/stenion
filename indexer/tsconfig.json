{
  // Sources + tests, no emit — what editors resolve files through.
  // See ../tsconfig.check.json. Emitting to dist/ is tsconfig.build.json.
  "extends": "../tsconfig.check.json",
  "include": ["src"]
}
