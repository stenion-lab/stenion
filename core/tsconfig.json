{
  // The default config: sources + tests, no emit. This is what your editor
  // resolves files through, so it must include *.test.ts — see
  // ../tsconfig.check.json for why. Emitting to dist/ is tsconfig.build.json.
  "extends": "../tsconfig.check.json",
  "include": ["src"]
}
